from setuptools import setup

setup(name='sortedwalk',
      version='0.1',
      description='Recursively traverse files in a root directory.',
      url='http://github.com/shotesmagotes/sortedwalk',
      author='Shota Makino',
      author_email='shota.makino@yahoo.com',
      license='MIT',
      packages=['sortedwalk'],
      zip_safe=False)